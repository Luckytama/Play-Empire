
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing how to use dependency injection
GET     /count                      controllers.CountController.count
# An example controller showing how to write asynchronous code
GET     /message                    controllers.AsyncController.message

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
#
GET     /                           controllers.HomeController.about

GET     /empire                     controllers.EmpireController.empire
GET     /empire/newgame             controllers.EmpireController.newGame

POST    /empire/startgame           controllers.EmpireController.startGame
POST    /empire/distribute          controllers.EmpireController.distribute
POST    /empire/getAttackTo         controllers.EmpireController.getAttackTo
POST    /empire/attack              controllers.EmpireController.executeAttack
POST    /empire/complete            controllers.EmpireController.completeRound
